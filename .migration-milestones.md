# Memory Migration Milestones

Non-destructive migration: OpenClaw builtin memory index (`main.sqlite`, text-embedding-3-small 1536d)
→ PluresLmStore (bge-small-en-v1.5 384d, re-embedded on write).

## Verified facts
- SOURCE: `C:\Users\kbristol\.openclaw\memory\main.sqlite` — table `chunks`, **1692 rows**.
  cols: id,path,source,start_line,end_line,hash,model,text,embedding,updated_at.
- READ method: `node:sqlite` (`DatabaseSync`, `{readOnly:true}`) — available & working on Node v26.3.0.
  No new npm deps needed; `better-sqlite3` NOT installed.
- TARGET store class: `PluresLmStore` from `./dist/pluresdb.js`.
  - `open({ dbPath, embeddingModel: "BAAI/bge-small-en-v1.5" })`
  - `store(nodes: Array<{id,data}>)` → `{written, skipped, refused, refusedDetail}` (batch, embed-on-write, idempotent).
  - embeddable text = `data.content || data.text || data.summary` → chunk text goes in `data.content`.
  - category = `data.category || data.type || data.kind` → set `category:"memory-chunk"`.
  - `count()`, `status()` → `{totalNodes, typeCounts, ...}`, `recall(query, limit)`.
- Idempotency: `store()` internally checks `#isDirty` (compares to `db.get(id)`) → re-run skips unchanged.

## Progress
- [x] script authored (migrate-memory.mjs + verify-migration.mjs)
- [x] dry-run 50 imported
- [x] dry-run verified (VERIFY_OK + search hits)

## Dry-run result (LIMIT=50 -> migrated-store-dryrun)
- READ method: node:sqlite DatabaseSync {readOnly:true} (confirmed working).
- store() signature used: `store([{ id, data }])` where
  data = { content:<chunk text>, category:"memory-chunk", path, source,
           start_line, end_line, hash, updated_at, origin:"main.sqlite" }.
  -> returns { written, skipped, refused, refusedDetail }.
- MIGRATION_DONE imported=23 skipped=0 errors=0 refused=27 processed=50.
  (27 refused = store's C-MEM-REDACT secret gate flagging high-entropy tokens in
   the MEMORY.md-stub chunks that dominate the first 50 rows. Honest refusals,
   recorded in refusedDetail, NOT silent drops. Expected for these rows.)
- VERIFY: count()=34 (23 chunks + 11 internal praxis_constraint nodes),
  embeddingDimension=384 (bge re-embed confirmed), hasEmbedder()=true.
  All 3 sample queries returned 3 hits (scores ~0.69-0.70). VERIFY_OK count=34.
- "Built vector index: 0 entries" log line is the documented-harmless alpha
  behavior (putWithEmbedding vectors are already searchable) — proven harmless
  because recall returns real scored vector hits.

## NOT done (main session's call)
- Full 1692-row import. Run without MIGRATE_LIMIT into
  TARGET_DBPATH=migrated-store (the non-dryrun dir). Idempotent re-run safe.
- NOTE: a meaningful fraction of chunks may be refused by the secret gate; that
  is by design. If those chunks must migrate, that's a policy decision about the
  gate, not a script bug.
