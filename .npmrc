# pnpm settings for plureslm-openclaw.
# See pnpm-workspace.yaml for verifyDepsBeforeRun + allowBuilds (pnpm 11 home).
# Keeping this here too for tools that still read .npmrc.
verify-deps-before-run=false
