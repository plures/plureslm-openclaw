# P2-ANALYZE Milestones

## Task: Design spec for P2 "Structural promotion signal" (consumes P3's salience)

- [x] Locate p3p4 worktree consolidate code — it's `src/pluresdb.ts` `consolidate()` @ L1219, NOT a separate consolidate.ts
- [x] Verify graph analytics: `graph_pagerank` @ L1297, `graph_clusters(louvain)` @ L1320 (IR ops, real native)
- [x] salienceForNode(id) DOES NOT EXIST — salience is `topRanked: string[]` in a durable checkpoint, not per-node
- [x] Grep blended_search_score — DOES NOT EXIST in this repo. Recall sort is pure `score` desc @ L926
- [x] P1's real blend = `seedScore × DECAY_PER_HOP^hops` for GRAPH hits only (P1 SPEC §3.2, L215)
- [x] Find recall scorer: `recall()` @ L891, sort @ L926; capability `search()` @ memory-capability.ts:274
- [x] Find Headroom/eviction — Headroom (H stage) is a SEPARATE transient ChatMessage[] compressor, NOT node eviction
- [x] Retention/decay-by-removal = HONEST ABSENCE @ L1204-1208 (augment-only, never calls native delete)
- [x] CRITICAL: `#readCheckpoint` @ L1151 DROPS topRanked on read → salience is WRITE-ONLY currently
- [x] pagerank_score returned in r.data @ L1305; allowlisted node keys structural_rank/pagerank_score @ L135-136
- [x] UNKNOWN resolved: salience for fresh nodes — topRanked is EMPTY until first sweep runs with ≥1 edge; reader drops it anyway
- [x] Assemble seam inventory (file:line) — DONE
- [x] Draft P2 design (salience-weighted recall + salience-protected retention) — DONE
- [x] Draft test/verify plan (real store boundary, C-TEST-002) — DONE
- [x] Draft NON-goals + risks/unknowns — DONE
- [x] Write SPEC.md — DONE
- [x] Report back
