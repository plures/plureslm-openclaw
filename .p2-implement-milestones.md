# P2 IMPLEMENT — Salience-Weighted Recall Consumption Layer

Worktree: `C:\Projects\plureslm-openclaw-p2`\nBranch: `feat/p2-structural-promotion` (off origin/main `bc4778c`)
Requester gates on THIS file + on-disk verification.

## Pre-flight

- `pnpm install` — done (was missing node_modules in worktree; pnpm v11.5.1, exit 0).
- Baseline `pnpm run build` → `BUILD_EXIT=0` (tsc clean).
- Baseline `pnpm test` → `TEST_EXIT=0`, final line `=== P3+P4 VERIFY RESULT: ALL CHECKS PASSED ===`.
  - Confirms P2-G0 empirically: baseline consolidate already emits non-empty
    `topRanked` (e.g. `["mem:session:cseed-9:0","mem:session:cseed-8:0","mem:session:cseed-7:0"]`)
    over an edged 24-node/276-edge session graph → pagerank varies by connectivity.

## Architecture notes discovered (trust-but-verified on disk)

- `recall()` (pluresdb.ts ~926) returns ONLY direct vector+text hits. **Graph
  expansion is NOT in recall()** — it happens downstream in
  `memory-capability.ts::createPluresLmSearchManager().search()` (~line 327+),
  which takes `hits.slice(0,3)` as seeds and APPENDS graph neighbors AFTER all
  direct hits, de-duped, never displacing a direct hit. → My salience change to
  recall()'s sort only reorders DIRECT hits among themselves; the P1
  precision guardrail (graph neighbor never displaces direct top-1) is
  structurally untouched.
- `#writeCheckpoint` persists `{lastRunEpoch,runs,edges,clusters,topRanked}`.
- `#readCheckpoint` (~1150) read back ONLY `{lastRunEpoch,runs}` → topRanked
  orphaned. This is the reader bug P2-0 fixes.

---

## Deliverable status

### P2-0 (reader fix): DONE — commit `b78da60`
- `#readCheckpoint` return type widened to `{lastRunEpoch,runs,edges,clusters,topRanked}`;
  reads back all persisted fields (defaults 0/0/0/0/[]), defensively filters
  `topRanked` to strings. Added `#salientIds(db): Set<string>` (persisted
  topRanked as a Set; NO pagerank recompute at read time).
- `pnpm run build` → `BUILD_EXIT=0`.
- `pnpm test` → `TEST_EXIT=0`; all of: recall `RESULT: ALL CHECKS PASSED`,
  `HARDENED RESULT: ALL CHECKS PASSED`, `QA REGRESSION RESULT: ALL CHECKS PASSED`,
  `P3+P4 VERIFY RESULT: ALL CHECKS PASSED`.

### Deliverable 2 (salience-weighted recall): DONE — commit `5cfd83c`
- Added module const `const SALIENCE_BONUS = 0.15;` (documented).
- `recall()` reads salient set once, sorts by `eff(h)=h.score + SALIENCE_BONUS*h.score*(salient.has(h.id)?1:0)`,
  keeps `.slice(0,k)`. `hit.score` NOT mutated. Empty-set → `eff==score` →
  comparator byte-identical to prior `b.score-a.score` (stable sort preserves
  ties). Graph hits appended downstream (memory-capability.ts) so P1 top-1
  guardrail untouched.
- `pnpm run build` → `BUILD_EXIT=0`.
- `pnpm test` → `TEST_EXIT=0`; recall / HARDENED / QA REGRESSION / P3+P4 VERIFY
  all `ALL CHECKS PASSED`.

### Deliverable 3 (retention protection): SKIPPED — HONEST ABSENCE (no eviction path exists)
See "Retention finding" below. Not stubbed, not fabricated.

### New gate test (test/p2-salience-recall-child.mts + wrapper): DONE — commit `8e8fc73`
- Added `test/p2-salience-recall-child.mts` (2 phases: `salient`, `invariant`) +
  `test/p2-salience-recall.gate.mts` (spawn-per-child wrapper, modeled on
  p3p4.gate.mts). Wired into `pnpm test` (appended to the `&&` chain) +
  `test:p2-salience` script.
- Real store only, channel-agnostic (C-TEST-002): edges/salience/order all from
  shipped code; nothing fabricated.
- **PROOF (a)** WITNESS (empirical, from shipped recall): salient `sseed-7`
  (raw score 0.87023, in topRanked) out-ranks non-salient `sseed-16`
  (raw score 0.87374, NOT in topRanked). sseed-7 wins with a LOWER raw score —
  only the +15% proportional bonus (eff=0.87023*1.15=1.00076 > 0.87374) explains
  it. topRanked was `[sseed-9,sseed-8,sseed-7,sseed-6,sseed-5]` over a
  24-node/276-edge session graph.
- **PROOF (b)** INVARIANT: direct `store.store()` seeding writes NO checkpoint
  → empty salient set → recall order `[inv:9,inv:7,inv:4,inv:8,inv:5,inv:1,inv:11,inv:3]`
  == independently-computed raw-score order (identical sequence) →
  `invariantHolds: true`.
- Standalone gate: 13/13 checks `P2 SALIENCE RESULT: ALL CHECKS PASSED`, exit 0.

### DRIFT FROM BRIEF (with evidence)
1. **Gate wrapper filename**: brief said name it `test/p2-salience-recall-child.mts`
   "+ a gate wrapper if the suite uses per-process children." The suite DOES use
   per-process children (spawnSync), so I added the child +
   `test/p2-salience-recall.gate.mts` wrapper and wired the WRAPPER into `pnpm test`
   (mirrors exactly how p3p4 gate files are invoked). No behavioral drift.
2. **Test A/B design**: brief's proof-(b) baseline. Initial design seeded the
   "plain" store with `manager.sync(force:true)` — but sync() OPPORTUNISTICALLY
   calls `store.consolidate({force})` (memory-capability.ts ~559), so that store
   was NOT salient-empty and the invariant "failed" (a TEST bug, not a product
   bug). Fixed by seeding the invariant store via the DIRECT `store.store()`
   path, which never consolidates → genuinely empty salient set. This is a more
   faithful isolation of the empty-set invariant. Evidence: the salient-store
   recall and the direct-store recall both come from the SAME shipped
   `store.recall()`; only the presence of a checkpoint differs.
3. **Salience change location**: brief said modify the sort in `recall()`
   (pluresdb.ts). Verified graph-expansion (`via:"graph"`) hits are appended
   DOWNSTREAM in `memory-capability.ts::search()` (not in recall()), so the P1
   precision guardrail (graph neighbor never displaces direct top-1) is
   structurally untouched by the recall() sort change. No drift — just confirming
   the guardrail lives one layer up and is unaffected.

---

## Retention finding

**Result: NO node-eviction / retention / compression / pruning path exists.
Deliverable 3 is an HONEST-ABSENCE SKIP (per the brief: "If there is no
eviction of nodes, DO NOT invent one").**

Evidence (exhaustive `Select-String` over all `src/*.ts` for
`.delete(` / `.remove(` / `evict` / `prune` / `compress` / `retention` /
`maxNodes` / `capacity` / `truncate` / `expire` / `tombstone` / `dropNode`):
the ONLY matches are two DOC-COMMENT lines inside `consolidate()`
(pluresdb.ts ~1281,1285) that explicitly state a decay/eviction step that
DELETES stale nodes is *intentionally NOT performed*. The two `.delete(`
code hits are unrelated to memory nodes:
- `PluresLmStore.#instances.delete(dbPath)` — evicts a memoized native HANDLE
  from a process-local `Map` cache (not a stored node).
- `delete db.__plmRealExecIr` — removes a JS test-shim property.

The shipped surface is AUGMENT-ONLY: the only native mutations are `put`,
`auto_link` (writes deterministic `edge::{from}::{to}` CRDT nodes), and
best-effort `buildVectorIndex`. It NEVER calls native `delete` (stated in the
file header ~line 18 and the consolidate docstring). "Decay" is honest absence
— a monotonic `runs` counter is the durable age signal a FUTURE eviction
policy could build on, but no removal happens today.

=> There is no path that could drop or compress a salient node, so there is
nothing to protect. Adding a retention mechanism just to "protect" it would be
fabricating a feature that does not exist (violates C-NOSTUB-001 and the
brief's explicit "do not invent one"). Skipped honestly.

## Commit SHAs (atop bc4778c, branch feat/p2-structural-promotion)
- P2-0 reader fix: `b78da60`
- Deliverable 2 salience-weighted recall: `5cfd83c`
- New gate test + wiring: `8e8fc73`

## FINAL GATE OUTPUT (full `pnpm test`, exit 0)
```
=== RESULT: ALL CHECKS PASSED ===            (recall gate)
=== RESULT: ALL CHECKS PASSED ===            (p3p4 gate)
=== HARDENED RESULT: ALL CHECKS PASSED ===
=== QA REGRESSION RESULT: ALL CHECKS PASSED ===
=== P3+P4 VERIFY RESULT: ALL CHECKS PASSED ===
=== P2 SALIENCE RESULT: ALL CHECKS PASSED ===
FULLTEST_EXIT=0
```
Each deliverable was also independently built (`BUILD_EXIT=0`) + full-suite
green at commit time. No assertion was weakened; no stub introduced
(C-NOSTUB-001). NOT pushed, NO PR, NO merge — commits left on the branch for the
requester to verify + push.

## SUMMARY
- P2-0 (reader fix): DONE — `#readCheckpoint` now reads back topRanked/edges/clusters; `#salientIds` accessor added.
- Deliverable 2 (salience-weighted recall): DONE — SALIENCE_BONUS=0.15, eff-score sort, empty-set invariant preserved byte-identically.
- Deliverable 3 (retention protection): SKIPPED — honest absence; no eviction/retention/compression path exists to protect against.
- New gate test: DONE — proves (a) salient>peer flip and (b) empty-set invariant, real store, wired into `pnpm test`.
