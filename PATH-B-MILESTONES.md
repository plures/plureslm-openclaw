# Path B Milestones - TASK-2026-06-29-PATHB

Stage gates: a stage cannot start until the prior stage's gate passes. verify CANNOT be skipped.

- [x] **analyze** - sync() seam + write payload/id scheme + slot-selection/fallback mechanism documented
  - gate: a concrete implementation spec (no code) that names real SDK/native methods only
  - result: Verified spec written - `sync({reason,force,sessionFiles,progress})` ingests session-file chunks via the SAME memoized embedder-backed PluresDatabase handle (`put` auto-embeds, then `buildVectorIndex`), node payload `{content,category,type,source,path,hash,...}` recallable by the existing read path; slot via `plugins.slots.memory="plureslm"`+`dbPath`+manifest `"kind":"memory"`, memory-core stays default/inert fallback. Spec: PATH-B-ANALYZE.md
- [x] **analyze GATE PASSED** (2026-06-29). Spec = PATH-B-ANALYZE.md (fully cited, no invented APIs).
  - **Scope decision (Paradox, made by orchestrator):** session-files-first + optional configured-source dir. Memory-doc (MEMORY.md/memory/*.md) file-WATCHER parity left ABSENT this pass (C-NOSTUB-001 - no half-watcher), additive later. sync() contract is satisfied by session-transcript ingest.
  - **Caught blocker:** manifest needs `"kind":"memory"` or host never selects plureslm for the slot.
- [x] **implement** - write path on PluresLmStore + sync() on manager; headers/manifest updated
  - gate: code compiles conceptually; no stubs; read path untouched/intact
  - result: DONE (2026-06-29). `PluresLmStore` promoted to read+write on the SAME memoized exclusive-lock handle: added `put(id,data):boolean` + `store(nodes):{written,skipped}` (dirty-tracked via native `get(id)` on `hash`/`mtimeMs`+`size`, best-effort `buildVectorIndex()` once after writes), actor renamed `plureslm-reader`→`plureslm`, header rewritten; `seedStoreForTests` untouched. `memory-capability.ts` now implements `sync({reason,force,sessionFiles,progress})` exactly per SDK - chunks each file (sha256 per chunk, 1-based line tracking), `put`s `mem:session:<stem>:<i>` / `mem:memory:<slug>:<i>` nodes `{content,category,type,source,path,hash,mtimeMs,size,startLine,endLine,timestamp}` through the shared handle, idempotent+cheap when clean, `progress` per file; `sync` wired into the returned manager; optional `sourceDir` config rescanned on `force:true`; `search()` honors stored source/lines. `openclaw.plugin.json` got `"kind":"memory"` (was missing - host would never select the slot) + `sourceDir` schema + new description. `index.ts` threads `sourceDir`, headers updated. `npx tsc --noEmit -p .` CLEAN (exit 0). NO STUBS (C-NOSTUB-001). Honestly ABSENT (documented, not stubbed): memory-doc file-WATCHER, `close()`, `flushPlanResolver`. Notes: PATH-B-IMPLEMENT-NOTES.md.
- [x] **implement GATE PASSED** (2026-06-29). Diff verified: real `db.put` on the memoized `newWithEmbeddings` handle (pluresdb.ts:437/453/440-461), `#isDirty` dirty-track, `buildVectorIndex`, `sync()` chunks→store (memory-capability.ts:340). Manifest `kind:"memory"` set. Stub scan clean. tsc exit 0.
- [x] **test** - build clean + recall gate + NEW write→recall round-trip green (Windows)
  - gate: `pnpm run build` ok AND all tests pass
  - result: DONE (2026-06-29). `pnpm install` skipped (node_modules present). `pnpm run build` (tsc) **exit 0**; `pnpm run check` (tsc --noEmit, type-checks tests) **exit 0**. Canonical tsx gate `pnpm run gate` → `RESULT: ALL CHECKS PASSED` (exit 0); vitest mirror `pnpm run test:vitest` → **3 passed** (exit 0). **GATE A** PASS (read path opens real on-disk store). **GATE B** PASS (seed→reopen cross-process→non-empty correct recall: top hit `mem-dec-1`, source/citation correct). **GATE C (NEW, write→recall)** PASS - exercises the SHIPPED path (`buildMemoryCapability → getMemorySearchManager → manager.sync()`, NOT seedStoreForTests): write child ran `sync({sessionFiles:[tmp]})` → `delta:1` (real `store→put→buildVectorIndex` wrote a node), `progressCalls:1`; a SEPARATE read process (lock released) recalled the sentinel `ZQX7731SENTINEL...` as node `mem:session:session-gatec:0`, `source:sessions`, score 1, via text, `totalNodes` durable across processes. The write→recall round-trip GENUINELY works through the shipped `sync()`. Fixes: GATE B exact-count assertions (`===3`) were wrong - the native bootstraps 10 `praxis_constraint` baseline nodes into every fresh store (totals are baseline+seeded); made counts baseline-tolerant (`>=3` + cross-process stable) while keeping all exact-recall checks, in BOTH gate files. Known limitation (honest, not a stub): `buildVectorIndex` populates 0 entries for freshly `put` nodes in this native build → semantic-only recall of a just-written node is 0; written nodes are text/substring-recallable, which GATE C uses and the brief permits. To validate in QA against a real store dir. Notes: PATH-B-TEST-NOTES.md.
- [x] **test GATE PASSED (orchestrator-verified)** (2026-06-29). build exit 0; tsx gate ALL CHECKS PASSED + vitest 3 passed; write→recall round-trip proven through shipped sync() (delta:1, cross-process sentinel recall). **Independently re-run by orchestrator** - GATE A/B/C all PASS on my own `pnpm test`. No assertion weakened: the `>=3` count fix is correct vs the real native bootstrap (10 `praxis_constraint` nodes in every fresh store, confirmed); every exact-recall correctness check (top-hit id, snippet content, source, citation) is intact.
- [x] **qa** - native loader read+write confirmed (Windows); root-cause vector-index-0-for-fresh-nodes; Linux loader path checked
  - gate: write+recall works against a real store dir via the shipped path; vector-fresh-node behavior explained (bug vs expected); no new stubs
  - **carry-in finding (from test):** `buildVectorIndex` reports 0 entries for freshly `put` nodes → sentinel recalled `via:text` not vector. Write→recall contract holds, but **semantic** recall of just-written nodes is currently degraded. QA MUST root-cause: is this an index-timing/lock artifact, an embed-on-put gap, or expected-needs-rebuild-pass? Determines whether P1 graph/vector work is blocked.
  - result: DONE (2026-06-29). **Vector verdict = (a) embed-on-put gap, a NATIVE-ALPHA LIMITATION (not a plugin bug)** - proven by `test/qa-vector-probe.mts`: embedder WORKS (`embeddingDimension()=384`, `embed(["hello world"]).length=384`), but after `put(id,{content})` neither `get(id)` nor `getWithMetadata(id)` carries a vector (`vectorLen:null`), `buildVectorIndex()=0`, and `vectorSearch=0` for the put node **both in-proc AND after a fresh reopen** (rules out (b) timing/reopen). The `putWithEmbedding` **control** (explicit precomputed vector, same session) → `vectorSearch=1` hit surviving reopen, and text `search=2` hits - so the index/search machinery is fine; only `put()` fails to auto-embed despite the native's own documented contract (rules out (c) by-design). `buildVectorIndex()`'s integer return is itself unreliable (returns 0 even when a putWithEmbedding vector is searchable). **Impact:** semantic recall of synced memory is degraded to TEXT-ONLY; P1 vector/graph over fresh writes is blocked on the native. Fix (deferred to verify/P1, not applied): fix native `put()` auto-embed (preferred, 0 plugin change) OR plugin writes via `putWithEmbedding(id,data,db.embed([text])[0])` (proven to restore vector recall). **(1) Real-store-dir read+write (cross-process), shipped path** = PASS: `sync()` of a real session file in one process (`delta=1`) → a SEPARATE process recalls sentinel `mem:session:qa-session:0` (`source:sessions`, score 1) durable across the lock boundary; store reports `vectorAvailable=true/dims=384` but sentinel comes back **via text** (consistent with (a) - disclosed, not hidden). **(2) `#isDirty` idempotency** = PASS: 2nd `sync()` of the same unchanged file added 0 nodes (`delta1=1`, `delta2=0`, progress still fired) - skip-on-clean proven. **(3) Linux loader** = honest ABSENT: only `pluresdb-node.win32-x64-msvc.node` exists (no Linux `.node` in the pkg or the `file:` source crate); loader code path is platform-generic but UNVERIFIED on Linux this pass (needs a Linux-built binary). **QA-found defect DEF-PATHB-1** logged (native `put` no-auto-embed). NO `src/` changes, NO new stubs (C-NOSTUB-001). Probes: `test/qa-vector-probe.mts`, `test/qa-store-probe.mts`. Notes: PATH-B-QA-NOTES.md.
- [x] **qa GATE PASSED** (2026-06-29). Native loader read+write confirmed on Windows against a real store dir via the SHIPPED path; cross-process exclusive-lock contract re-confirmed beyond fixtures; idempotency proven (delta2=0); vector-fresh-node behavior fully root-caused = (a) native-alpha embed-on-put gap (control `putWithEmbedding`=1 hit proves machinery + embedder are fine; reopen rules out timing). Semantic recall of synced memory honestly flagged as text-only / blocked-on-native (DEF-PATHB-1) with a concrete tested fix deferred to verify/P1. No stubs.
- [x] **implement-fix GATE PASSED** (2026-06-29). DEF-PATHB-1 fix applied to `src/pluresdb.ts` write path ONLY: write path now embeds text explicitly (`#embeddableText` content→text→summary, `#embedForWrite` = `db.embed([text])[0]` with dim-match guard) and persists via `db.putWithEmbedding(id,data,vec)` in a new `#writeNode`, called AFTER the unchanged `#isDirty` skip (dirty-first; unchanged nodes never embedded); honest text-only `db.put` fallback only when no embedder / empty text / embed-fail. `store()` still best-effort `buildVectorIndex()` (return NOT relied on). read path, `memory-capability.ts` sync(), node schema/ids, `seedStoreForTests` UNTOUCHED. **PROOF - vector recall of a node written through the SHIPPED `sync()` path: BEFORE=0 (QA), AFTER=1.** `test/qa-vector-after-fix.mts` (2 processes): write leg `sync()` `delta:1`; separate process `db.vectorSearch(db.embed([query])[0],5,0.0)` → `vectorHitCount:1`, top `mem:session:vecfix-session:0`, `sentinelInVector:true`, score 0.7957; shipped `manager.search()` returns it `via:vector` (0.7957); driver `VECFIX_RESULT: PASS`. Canonical gate: `pnpm run build` exit 0; `pnpm test` **GATE A/B/C ALL PASS** - GATE C cross-process sentinel now recalled **`via:vector`** (0.8565) where pre-fix it was `via:text`, no assertion weakened. NO STUBS (C-NOSTUB-001). Notes: PATH-B-QA-NOTES.md §"DEF-PATHB-1 FIX".
- [x] **verify** - slot flip: provider=plureslm when configured; memory-core fallback when not
  - gate: status() reports provider=plureslm; graceful fallback verified (channel-agnostic)
  - result: DONE (2026-06-29). **BOTH PROOFS HOLD** - channel-agnostic (C-TEST-002, NO chat adapter; via shipped `buildMemoryCapability → runtime.getMemorySearchManager → manager.*` + config + bounded host greps), no stubs (C-NOSTUB-001), no real `openclaw.json` mutated (throwaway store dirs only). Harness `test/verify.driver.mts`+`test/verify-child.mts` → **ALL CHECKS PASSED (exit 0)**. **PROOF 1 (plureslm owns the slot when configured):** seeded a sentinel through the shipped `manager.sync({sessionFiles})`, reopened cross-process; `status()` = `{backend:builtin, provider:"plureslm", model:BAAI/bge-small-en-v1.5, chunks:11, vector.dims:384}`, sentinel recalled **via VECTOR** score 0.7933 as `mem:session:verify-session:0` (source:sessions, citation `plureslm:session:…`). **PROOF 2a (no dbPath):** `getMemorySearchManager` → `{manager:null, error:"[plureslm] no dbPath configured (plugins.entries.plureslm.config.dbPath); memory capability is inert."}` (no crash). **PROOF 2b (bad dbPath = parent-is-a-file):** `{manager:null, error:"[plureslm] failed to open store at …: [STORAGE_OPEN_FAILED] IO error … (os error 183) …"}` - `threw===false`, graceful, no stub, no fake. **Host-selection evidence (installed dist):** `slots-kpL659LX.js:6` `DEFAULT_SLOT_BY_KEY.memory="memory-core"`; `memory-runtime-BnrWbfn1.js:10-16` unset/disabled slot ⇒ `[]` (no plugin); `loader-CXafBhxY.js:530` treats `""`/`"none"`/`"memory-core"` as no-plugin + `:547` `hasKind(...,"memory")` gate; `doctor-memory-search-D0vqCTRc.js:108` + `model-context-tokens-CjCn2EKc.js:394` consumers degrade on null manager. Operator config to flip + revert note in PATH-B-VERIFY-NOTES.md. Notes: PATH-B-VERIFY-NOTES.md.
- [x] **verify GATE PASSED** (2026-06-29). Both proofs proven channel-agnostically; loop closed. provider=plureslm with vector recall when configured; honest `{manager:null,error}` (no-dbPath AND bad-dbPath) drives the host's real memory-core fallback (DEFAULT_SLOT_BY_KEY.memory="memory-core" + null-manager degrade, cited file:line). No stubs, no live-config mutation.
- [x] **implement-fix GATE PASSED (orchestrator-verified)** (2026-06-29). DEF-PATHB-1 fixed via explicit `putWithEmbedding` in `#writeNode` (pluresdb.ts:385-389), embed validated against `embeddingDimension()`, dirty-first so clean nodes skip embed. **Independently re-run**: `pnpm run build` exit 0; `pnpm test` = ALL CHECKS PASSED; GATE C sentinel now recalled `via:vector` score 0.8565 (was `via:text`), provider=plureslm, durable cross-process. Vector hits for a synced node 0 (pre-fix) -> 1 (post-fix). No stubs. Semantic recall genuinely works through the shipped sync() path.

## Notes
- compile_nl + native loader were ALREADY real (verified 2026-06-29) - out of scope.
- Real gap = MemorySearchManager.sync (write path). Seam: memory-state-FIOhoe_D.d.ts:118.
