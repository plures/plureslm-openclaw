# H — Headroom Token-Compression Port (EPIC-MEMORY-SUPERIORITY)

Spec: epic/H-headroom-port-SPEC.md. Port the pares-agens token-compression (Headroom) capability into the memory plugin.
⚠️ C-NOSTUB-001 LANDMINE (from spec): the agens `.px` Headroom "strategy suite" (~160 actors) is a STUB FARM returning canned `{"compressed":true,"ratio":0.5}`. The REAL compressor is the Rust HeadroomHook / headroom_bridge.rs. Port the REAL bridge logic, NEVER the .px shell.
⚠️ Forbidden edge: radix/pluresLM must NEVER depend on pares-agens. Port the algorithm; do not add an agens dependency.
Stage gates: a stage cannot start until the prior stage's gate passes. verify CANNOT be skipped.

- [x] **analyze** — locate + read the REAL Rust compressor (HeadroomHook/headroom_bridge.rs/executor.rs); define the surface for the plugin (TS-reimpl of the real algorithm vs a new NAPI on @plures/pluresdb-native — spec flagged the native trait is NOT JS-exposed today); confirm the real compression algorithm (not the canned ratio); define the seam (where compression runs: flush/compaction) + a measurable compression-ratio/fidelity metric.
  - gate: impl spec (no production code) citing the REAL Rust source lines; explicit decision TS-reimpl vs NAPI with rationale; NO reference to the .px stub farm as the impl.
  - result: DONE (2026-06-29). Real compressor = `pares-agens/crates/core/src/headroom_bridge.rs` `HeadroomHook` (content-type-routed TRANSIENT byte compression: prose/error head+tail sentence window, code AST-signature skeleton, log dup-run collapse `line [×N]`, json/other whitespace squeeze; real token counts via `tiktoken_rs::cl100k_base` in headroom.rs). Verified on-disk: compress_one/compress_prose/compress_code/collapse_whitespace all present; `.px` ~160-actor stub farm (`{"compressed":true,"ratio":0.5}`) explicitly flagged DO-NOT-PORT (#1 risk). **Decision = NEW NAPI on `@plures/pluresdb-native` (pluresdb-node crate): `compressText`/`countTokens`/`detectContentType`, thin TS message-loop/gate/guard.** Dependency-boundary PROVEN: `grep pares-agens pluresdb/crates/*/Cargo.toml` = EMPTY; pluresdb-node already deps pluresdb-px (owns ActionHandler trait) + pluresdb-core (CrdtStore+embedder); actors need only tiktoken_rs/unicode_segmentation/sha2/CrdtStore. Seam = src/memory-capability.ts sync() at `chunks.map(...content...)` before store.store(). Spec: H-ANALYZE.md.
- [x] **analyze GATE PASSED (orchestrator-verified)** (2026-06-29). On-disk confirmed: HeadroomHook L79 + compress_one L212 + compress_prose L259 + compress_code L299 in headroom_bridge.rs; real cl100k via tiktoken_rs in headroom.rs; pares-agens absent from all pluresdb crate manifests; pluresdb-node already deps px+core. NAPI surface respects the forbidden edge (no agens dep). **Note: implement stage modifies C:\Projects\pluresdb (Rust + native rebuild) — SEPARATE repo from P1/P3/P4 src/, so Headroom runs fully parallel to the src/ chain.** PASS — epic/H-ANALYZE.md (403 lines). REAL compressor = pares-agens/crates/core/src/headroom_bridge.rs `HeadroomHook::compress_one` (L231-258) + 4 strategies: prose head+tail sentence window (compress_prose L266-301), code signature skeleton (compress_code L310-342), log dup-run collapse (compress_log L349-383), json/other whitespace squeeze (collapse_whitespace L406-422); calls 5 REAL pure-Rust actors in headroom.rs (detect_content_type L68-86, split_sentences L156-166, extract_ast_signatures L169-173, detect_language, pluresdb_write/read); real tokens via tiktoken cl100k (headroom.rs L97-103). DECISION: add NEW NAPI on @plures/pluresdb-native (pluresdb-node) — NOT pure-TS, NOT an agens dep. PROOF clean: pluresdb-node/Cargo.toml already deps pluresdb-px (owns ActionHandler trait, executor.rs L33) + pluresdb-core (CrdtStore/embedder); `Select-String pares-agens C:\Projects\pluresdb\crates\*\Cargo.toml` = empty; plugin package.json no agens ref. SEAM: src/memory-capability.ts sync() at `nodes = chunks.map(...content: chunk.content...)` before store.store(nodes); lower seam src/pluresdb.ts #writeNode. METRIC: cl100k ratio + fidelity (head/tail sentinel retention + signature/distinct-line preservation) on REAL text, never the .px `0.5`. Stub farm (~160 canned actors + headroom-strategies/*.px) explicitly flagged DO-NOT-PORT (#1 risk).
- [x] **implement** — real compressor in the chosen surface; wired into the plugin's flush/compaction seam; no stubs, no canned ratios, no agens dep.
  - gate: tsc/build clean; no stubs; real algorithm present; diff-verified.
  - result: DONE (2026-06-29). PASSED, orchestrator-verified on my own invocation against the rebuilt native (pluresdb-node.win32-x64-msvc.node, 36MB, rebuilt 4:06PM). REAL ported compressor (compress_one router + 4 real strategies + 5 real actors; `.px` stub farm NOT ported). 3 NAPI methods live + callable from JS: compressText/countTokens/detectContentType. PROVEN real token reduction (cl100k, never 0.5): prose 181→38 (0.21, head+tail kept/middle elided), log 128→95 (0.742, real `[×3]` run-collapse marker in output), short-guard 2→2 shrank:false (net-savings guard HOLDS). No-agens-dep PROVEN: only tiktoken-rs 0.6 + unicode-segmentation 1.12 added; zero pares-agens/pares-radix (the one manifest mention is an explanatory comment). napi build --release exit 0; 7/7 Rust unit tests pass. Notes: epic/H-IMPLEMENT-NOTES.md.
  - ⚠ FLAGGED FOR TEST STAGE: `detectContentType` returned "prose" for a repeated-line log sample (auto-detector mis-flag). Compression still correct because type was passed explicitly, but the detector's robustness needs a test-stage assertion. Not an implement-gate blocker (algorithm+NAPI are real); a real accuracy follow-up. DONE (2026-06-29) — native Rust + NAPI binding scope (the TS `src/` seam is a LATER stage; deliberately untouched per task scope). Ported the REAL pares-agens compressor into `@plures/pluresdb-native` (`pluresdb-node`) as a new dependency-free module `src/headroom.rs` + 3 `#[napi]` free fns in `lib.rs`: **compressText**(content, contentType?) / **countTokens**(text) / **detectContentType**(content). Faithful port of `compress_one` (headroom_bridge.rs L231-258) + 4 strategies (prose head+tail window L266-301, code signature skeleton L310-342, log `[×N]` run-collapse L349-383, json/other whitespace squeeze L406-422) + 5 real actors from headroom.rs (detect_content_type L68, real cl100k `count_tokens` L97 cached in OnceLock, split_sentences L156 via unicode-segmentation, detect_language_heuristic L577, extract_signatures_heuristic L600) + the is_log/is_code/looks_like_error heuristics. The `ActionHandler` JSON indirection was collapsed to direct calls; the ~160-actor `.px` STUB FARM was NOT ported. Added crate-local deps `tiktoken-rs=0.6` + `unicode-segmentation=1` (neither is agens). **BUILD: `cargo check` clean; 7/7 Rust unit tests pass; `napi build --release` exit 0 (3m32s) → pluresdb-node.win32-x64-msvc.node + regenerated index.js/index.d.ts.** No stubs, no `todo!()`, no canned ratio. NO-AGENS PROOF: `cargo tree -p pluresdb-node` shows only tiktoken-rs/unicode-segmentation added, zero pares-agens/pares-radix; the only `pares-agens` string in `pluresdb/crates/*/Cargo.toml` is an explanatory comment, not a dep. Notes: epic/H-IMPLEMENT-NOTES.md.
- [x] **test** — build + a compression round-trip on real text: assert real token reduction AND fidelity (decompressed/important content preserved per the algorithm's contract), not a hardcoded ratio.
  - gate: real measured compression on real input; orchestrator-verified.
  - result: DONE (2026-06-29). **`cargo test -p pluresdb-node` = 16 passed / 0 failed / 0 ignored on my own run** (first run had 2 REAL failures I fixed for real — a sub-floor json fixture + the `[ERROR]`→json ordering gap — NOT by weakening assertions). Native rebuilt: `napi build --platform --release` exit 0 (latest rerun: 2.16s). Re-ran a node probe against the rebuilt `.node` — REAL cl100k ratios (compressed/baseline, never hardcoded): **prose 0.162** (641→104; head+tail kept, middle elided), **code 0.535** (144→77; every signature kept, bodies dropped), **log 0.080** (742→59; `[×30]`+`[×12]` run markers, distinct INFO preserved), **json 0.809** (94→76; whitespace squeeze). **NET-SAVINGS GUARD asserted + holds**: short 4→4 unchanged=true, incompressible 320→320 unchanged=true — never grows (the contract). **DETECTOR DEFECT VERDICT = FIXED (faithful superset)**: the orchestrator's repeated/level-prefixed log (`ERROR …`×N) now detects as `log` not `prose`, in a Rust unit test AND through rebuilt JS `detectContentType`. Root cause was a REAL gap inherited from pares-agens `is_log_content` (only matched `" ERROR "` mid-line, not line-leading); fix adds `is_level_prefixed` (line-leading/bracketed level tokens) + `starts_like_bracketed_log` carve-out — strict superset, zero false-positives on prose (`ERRORS`/`INFORMATION` pinned safe), cannot reclassify real JSON. Two genuinely-ambiguous residual cases PINNED as documented limitations (not hidden): bare repeated lines (no level/timestamp)→prose, and `[<timestamp>]`-prefixed logs→json — both share pares-agens' gap, both still compress correctly + losslessly when `Some("log")` is pinned. Fidelity spot-checked vs pares-agens source: compress_prose L266-301, compress_code L310-342 + extract_signatures_heuristic L600-657, compress_log L349-383 (`  [×N]` marker), collapse_whitespace L406-422, compress_one guard L231-258. No stubs, no canned ratio, no agens dep added. Notes: epic/H-TEST-NOTES.md.
- [ ] **qa** — exercise on realistic memory/session content; confirm ratio + fidelity hold; confirm no agens runtime dependency pulled in.
  - gate: works on real content; dependency boundary clean; no new stubs.
  - result:
- [ ] **verify** — compression metric (ratio + fidelity) computed channel-agnostically (C-TEST-002) on representative input; integrated path demonstrated. Closes the loop.
  - gate: measurable real compression demonstrated end-to-end.
  - result:

## Notes
- Decision pending analyze: TS-reimpl of the real bridge algorithm vs new NAPI binding. Spec leans on "native trait not JS-callable today" → likely TS-reimpl unless a NAPI is cheap+correct.
