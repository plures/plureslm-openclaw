# P1 — Graph-native associative recall · MILESTONES

Stage tracker for EPIC-MEMORY-SUPERIORITY / P1 (associative graph recall).

- [x] **analyze** — spec written + orchestrator-verified (`P1-ANALYZE.md`). PASSED.
- [x] **implement** — link-on-write + graph recall expansion landed; build+gates green; real edge proven via shipped sync() path.
- [x] **test** — GATE D (associative recall) promoted into the gate suite (standalone tsx + vitest mirror); associative-win + precision-guardrail asserted via the shipped path; full A/B/C/D green. PASSED.
- [x] **qa** — independent cross-process QA of the SHIPPED associative-recall behavior against a REAL store + the associative-recall metric. Edges DURABLE cross-process (6==6), re-sync does NOT duplicate edges (6==6, 0 new nodes), associative-only recall delta = **2**, precision guardrail HELD, graph expansion best-effort (forced error → search() still returns direct hits). NO defects. PASSED.
- [x] **verify** — FINAL P1 gate: channel-agnostic capability-contract proof through the REAL `MemorySearchManager` boundary (`getMemorySearchManager` → `manager.sync`/`search` ONLY, no internal store access, no chat adapter). DIRECT-MISS/GRAPH-HIT ✓ (disjoint ROTA surfaced `via:"graph"`), PRECISION preserved ✓ (BACKUP top-1 direct), DURABILITY ✓ (fresh-process win), GRACEFUL FALLBACK ✓ (lone node → direct top-1, 0 graph hits). Regression A/B/C/D ✓ (build exit 0). NO integration defect. PASSED. **P1 CLOSED.**

## verify result line

**VERIFY ✓ (2026-06-29) — P1 CLOSED.** FINAL P1 gate: channel-agnostic, capability-contract proof that associative recall works END-TO-END through the REAL OpenClaw memory-capability surface — driven ONLY through `buildMemoryCapability(cfg).runtime.getMemorySearchManager({cfg,agentId})` → `manager.sync()`/`manager.search()` (verified vs the shipped SDK `MemoryPluginRuntime`/`MemorySearchManager` contract). NO internal store method used for any assertion (no `store.neighbors`/`execIr`/`createPluresLmSearchManager`); NO chat adapter (C-TEST-001/002); NO fabricated edges/recall; NO stubs. Realistic same-session fixture (oncall escalation runbook + disjoint pager ROTA + postgres BACKUP + PLANTS control). **(1) DIRECT-MISS/GRAPH-HIT:** strict 0.80, fresh process — ONCALL direct vector rank0 (0.8165); disjoint ROTA (below the bar, no shared vocab) surfaced by `manager.search()` `via:"graph"`, citation `plureslm:graph:mem:session:sess-oncall:0->mem:session:sess-rota:0`, seeded from ONCALL, ranked strictly below it — association surfaced what cosine alone missed. **(2) PRECISION PRESERVED:** default threshold, fresh process — on-topic BACKUP top-1 direct (0.8603), no graph hit at top-1. **(3) DURABILITY:** the win was produced by a FRESH-process `manager.search()` reopening the same dbPath (edges read from disk, not RAM). **(4) GRACEFUL FALLBACK:** lone-node store (no sibling → no edge) → `manager.search()` still returned the memory as direct top-1 (0.8048), `graphHitCount=0` — augment-not-replace; P1 can never make recall worse than baseline. **Regression backstop:** `pnpm run build` exit 0 + `pnpm test` GATES A/B/C/D ALL PASS (exit 0). One transient cold-start child-timeout flake on the very first run (GATE D green in it; unrelated to associative recall; did NOT reproduce across 10+ runs) — characterized, not a logic defect, nothing weakened. **NO integration defect found** — capability behaves identically at the real manager boundary as the harness/QA indicated. Harness: `test/p1-verify-gate.mts` + `test/p1-verify-child.mts` (additive; Path B `verify-child.mts` left intact). 30/30 checks pass, deterministic across 4 runs. See `P1-VERIFY-NOTES.md`.

## implement result line

**IMPLEMENT ✓ (2026-06-29)** — link-on-write + graph recall expansion landed via the shipped path. `src/pluresdb.ts` (execIr/linkRecent/neighbors + `_edge` recall guard + `via:"graph"`), `src/memory-capability.ts` (sync() stamps numeric `data.syncEpoch`, calls `linkRecent` ONCE post-loop when written; search() appends top-3-seed depth-1 graph neighbors as `via:"graph"`, de-duped, after direct hits). IR field names confirmed vs real engine (`ir.rs`/`graph.rs`/`filter.rs`) — all matched EXCEPT the spec's ISO-string `timestamp >=` pre-filter, which is always-empty against `ops/filter.rs` (`compare_numeric` rejects String fields, no string-order fallback); corrected to a numeric `syncEpoch >=` (proven: ISO filter → 0 edges; numeric → edges form). Build `tsc` exit 0 + `tsc --noEmit` exit 0. **GATE A/B/C: ALL CHECKS PASSED** (read + Path B intact). Edge proof: `link` smoke → real `temporal` edge `edge::...alpha:0::...beta:0`, `neighbors(alpha)` returns beta; `verify-strict` smoke (fresh proc, threshold 0.80) → beta surfaced end-to-end via `search()` as `via:"graph"`. Honestly absent: cosine `semantic` edges (v1 = category+temporal), P3 reactive trigger, `close()`. No stubs (C-NOSTUB-001). Smoke kept at `test/p1-smoke.mts`. See `P1-IMPLEMENT-NOTES.md`.

## qa result line

**QA ✓ (2026-06-29)** — independent QA of the shipped associative recall against a REAL store, cross-process, plus the associative metric. Built first (`tsc` exit 0); regression baseline **GATES A/B/C/D ALL PASS** (`pnpm test` exit 0, confirmed twice). Drove ONLY the shipped `sync()`/`search()`/`store` API (C-TEST-002); no stubs, nothing weakened. **(2) Persistence cross-process:** fresh `.tmp/` dbPath, proc#1 `sync()` formed **6** real edges (`graph_links`), `neighbors(alpha)`=`[beta,delta,gamma]`; FRESH proc#2 reopened SAME dbPath → edges DURABLE on disk (**6 == 6**, identical neighbor set) and `via:"graph"` recall durable (strict 0.80: alpha direct rank0, beta/delta/gamma `via:"graph"` ranks1–3). Structural probe: 4 content nodes + **6 full-mesh `_edge` nodes** (C(4,2)), well-formed `edge::{from}::{to}` ids, **no edge leaks into recall**. **(3) Idempotent re-link:** re-sync unchanged files (fresh proc) → **0** new content nodes (`#isDirty`), edge count **before 6 == after 6** (deterministic id ⇒ last-writer-wins, NOT N copies), neighbor sets identical. **(4) Associative metric:** at strict 0.80, direct=`[alpha]`, graph=`[beta,delta,gamma]` → **associative-only recall delta = 2** (`beta`,`delta` surfaced ONLY via graph, below the vector bar). Guardrail re-confirmed (default threshold): gamma top-1 unchanged & direct, no graph hit at top-1, every graph hit ranks strictly below its seed. **(5) Sanity:** forced `neighbors()`/`execIr` to throw → `search()` did NOT throw out, returned 4 direct hits (gamma present). **No defects found.** Harness: `test/qa-orchestrator.mts` + `test/qa-assoc-child.mts` + `test/qa-structural-probe.mts` (real shipped path, no fabricated recall). See `P1-QA-NOTES.md`.

## test result line

**TEST ✓ (2026-06-29)** — associative-recall proof promoted into a first-class gate (**GATE D**) in BOTH runners: `test/recall.gate.mts` (standalone tsx; `runAssocChild` + `gateD()`) and `test/recall.gate.test.ts` (vitest mirror `D.`, matching the A/B/C pattern), driven by a new cross-process child `test/assoc-child.mts` (`link`/`read-strict`/`read-default`, each own process → exclusive-lock contract honored). GATE D asserts BOTH through the SHIPPED `sync()`+`search()` (no fabricated recall, C-TEST-002): **(a) ASSOCIATIVE WIN** — disjoint BETA (cosine <0.80 to the query, NOT a direct hit) surfaced at rank 1 as `via:"graph"`, citation `plureslm:graph:...alpha:0->...beta:0`, seeded from the alpha direct hit, durable across a fresh process; **(b) PRECISION GUARDRAIL** — with the default threshold the on-topic direct hit (GAMMA) ranks **top-1 unchanged**, no graph hit at top-1, every graph hit ranks strictly below its seed. **Precision guardrail HELD** (top-1 unchanged, no regression). Build exit 0. **GATES A/B/C/D: ALL CHECKS PASSED** on `pnpm test` (standalone tsx) AND `vitest run` (4/4). Existing `p1-smoke.mts` `link`+`verify-strict` still pass. No assertion weakened; no stubs; no real regression found. Measured: alpha direct 0.8525 (vector) / beta-via-graph rank 1 (strict); gamma direct top-1 0.8646 (default). See `P1-TEST-NOTES.md`.
