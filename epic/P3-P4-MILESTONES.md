# P3 + P4 — Reactive in-DB consolidation + .px-governed write redaction · MILESTONES

Stage tracker for EPIC-MEMORY-SUPERIORITY / P3 (reactive consolidation sweep) + P4 (.px-governed write redaction).

- [x] **analyze** — spec written + orchestrator-verified (`P3-P4-reactive-governed-SPEC.md`). PASSED.
- [x] **implement** — P4 governed write (detectSecret → native pxOnAction ActionBlocked, fail-closed, whole-chunk refusal reported) + P3 consolidate() (execIr filter+aggregate → idempotent auto_link → pagerank+clusters → durable checkpoint w/ run counter, 60s interval + force, opportunistic from sync()). Build+gates green. See `P3-P4-IMPLEMENT-NOTES.md`. PASSED.
- [x] **test** — P3/P4 gates hardened as first-class adversarial tests; security guarantees hold; 2 real detector false-positives found AND fixed (non-weakening). Build+full suite+vitest+tsc green on this run. See `P3-P4-TEST-NOTES.md`.
- [x] **qa** — adversarially attacked the redaction gate harder than the test matrix; found + FIXED one REAL secret-leak defect (secret in a secondary content field `value`/`body`/`note`/arbitrary, with a benign `content`, was written then recallable) at the gate chokepoint, non-weakening; detector held against all multiline/obfuscation evasions (0 leaks); FP-pressure stayed low; consolidate idempotent + bounded + durable at 40-node scale; chunk-split pinned as honest limitation (never reassembled by recall). New `p3p4-qa.gate.mts` (28 checks) + vitest mirror wired into the suite. Build+full suite (4 gates)+vitest(5)+tsc green on this run. See `P3-P4-QA-NOTES.md`. PASSED.
- [x] **verify** — proved BOTH P3+P4 deliver real end-to-end value the way a consumer uses them, channel-agnostic (C-TEST-002: drove the SHIPPED `MemorySearchManager` via `buildMemoryCapability().runtime.getMemorySearchManager().manager` → `sync()`/`search()`; NO chat adapter, NO mock, NO private-state peeking for the safety proof), each reproduced across a FRESH process. C-MEM-REDACT end-to-end: a realistic batch (clean + secrets in `content`/secondary-`value`/multiline-PEM-`note`/AWS-40-`body`) synced via `manager.sync()` → in a fresh process `manager.search()` proved every credential node NEVER recalled + the raw secret NEVER in any snippet (incl. the secondary-field case the QA stage fixed, + a broad catch-all query) while the clean memory IS recalled; write-accounting `{written:0,refused:1}`+kind per secret, `{written:1}` clean. Consolidation: 24 nodes → 276 edges (complete same-session graph, bounded), STABLE across 6 sweeps (idempotent), run counter 2→7 then DURABLE 7→8 across a fresh reopen, 23 associative neighbors before+after restart. Honest PULL/TICK-not-push boundary stated (binding has no push path; `subscribe()` id-only stub; sweep opportunistic from `sync()`). Build+full suite (5 gates incl. new `p3p4-verify.driver`)+vitest(6)+tsc green on this run. See `P3-P4-VERIFY-NOTES.md`. PASSED.

## test result line

**TEST ✓ (2026-06-29)** — P3/P4 gates promoted to a repeatable adversarial suite driving ONLY the shipped `store`/`sync`/`recall`/`consolidate` API (C-TEST-002; every block proven by `get()===null` + recall MISS, every write by `get()!==null` + recall HIT). `pnpm run build` exit 0; `pnpm test` (recall.gate + p3p4.gate + **new p3p4-hardened.gate**) → **ALL CHECKS PASSED, exit 0**; `vitest run` → **4 passed, exit 0**; `tsc --noEmit` exit 0.

**P4 redaction matrix (direct `store()` AND chunk-level `sync()`): `CONFUSION MATRIX TP=11 FP=0 TN=7 FN=0`, `secretLeaks=0`, `cleanRecalled=7/7`.** All 11 real secret shapes (AWS AKIA, PEM private key, GitHub `ghp_`/`gho_`, Google `AIza`, Slack `xoxb-`, Stripe `sk_live_`, OpenAI `sk-proj-`, JWT, Azure `AccountKey=`, `password=` assignment) detected + refused + recall-missed via both paths. All 7 clean decoys (prose w/ "password/secret/key", sha256 hex, git sha, **base64 PNG blob**, code w/ `apiKey` name no value, runbook prose, **semver + UUID**) written + recallable, no secret rides along. **The matrix CAUGHT 2 genuine detector false positives** (base64 PNG blob + UUID wrongly flagged `high-entropy-token`); fixed for real in `src/redact.ts` with precise non-weakening carve-outs in `looksLikeOpaqueSecret` (`UUID_RE` anchored 8-4-4-4-12; `BASE64_FILE_MAGIC_RE` PNG/JPEG/GIF/PDF/ZIP/GZIP) — proven non-weakening: a bare 56-char base64 secret WITHOUT a magic prefix is still flagged, all 11 shapes still detect.

**P4 fail-closed:** governance latch forced CLOSED via real seam (`_forceGovernanceFailedForTests` sets `#governanceReady=false`, the precondition a thrown `pxInsertConstraint` produces — the real `#gateWrite` branch makes the decision) → secret REFUSED, `get()===null`, clean sibling still written; positive path (governance restored) → SAME secret still refused by the native engine. A secret never slips through because the rule was unavailable.

**P3 idempotency + durability:** 3× `consolidate({force})` over same store → **edges STABLE e1=e2=e3=3** (no duplication), pagerank+louvain ran each sweep; run counter monotonic 2→3→4; **DURABLE across a FRESH process** (reopen same dbPath → first run 7 > prior last 6); best-effort proven — poisoned internal `execIr` (`_poisonExecIrForTests`) → `consolidate` returned `{ran:false,reason:"empty"}`, did NOT throw out, store healthy after.

Added: `test/p3p4-hardened-child.mts` + `test/p3p4-hardened.gate.mts` (TP/FP/TN/FN matrix + all assertions, self-fails non-zero on any breach), vitest mirror in `test/recall.gate.test.ts` (spawns runner, asserts exit 0 + clean matrix + 0 leaks), `package.json` `test`/`test:p3p4-hardened`. Two TEST-ONLY seams in `src/pluresdb.ts` (documented doubles at real seams, C-NOSTUB-001 item 3 — inject the precondition only; block/best-effort decisions stay the real shipped logic). Detector fix in `src/redact.ts`. No `node_modules`/`.node` committed. No stubs (C-NOSTUB-001). No weakened assertions.

**Verdict: every secret shape blocked + recall-missed (11/11, 0 false-neg); 0 false positives on clean inputs (after fixing the 2 the gate caught); 3× consolidate edge-stable; run counter durable cross-process; full suite green.** TEST gate: **PASS**.

## qa result line

**QA ✓ (2026-06-29)** — Adversarial QA drove ONLY the shipped API (C-TEST-002). `pnpm run build` exit 0; `pnpm test` (recall.gate + p3p4.gate + p3p4-hardened.gate + **new p3p4-qa.gate**) → **ALL CHECKS PASSED, exit 0**; `vitest run` → **5 passed, exit 0**; `tsc --noEmit` exit 0.

**REAL DEFECT FOUND + FIXED (secret leak, safety-critical): secondary-field secret evasion.** The gate's `#gateWrite` inspected only `#embeddableText(data) || deriveSnippet(data)` — a SINGLE prioritized snippet (`content`→`text`→`summary`). Because of the `||` short-circuit, when a benign `content` was present, a live credential placed in another recall-surfaced field (`value`/`body`/`note`, or an arbitrary content field) was **NEVER scanned, written, and then recallable** — proven: 4/4 such cases persisted (`get()!==null`) with the GitHub/AWS/PEM token in the payload (`test/qa-gate-field-probe.mjs`). **Fixed at the chokepoint** (`src/pluresdb.ts`): new `#gateScanText(data)` scans EVERY content-bearing string value recursively, EXCLUDING structural/bookkeeping keys (`STRUCTURAL_NONCONTENT_KEYS`: `hash`/`category`/`type`/`source`/`path`/ids/line-numbers/sizes/timestamps/graph fields) so id-shaped values don't manufacture entropy FPs. Post-fix: all 4 cases REFUSED with correct kind + never recalled; clean control still written. **Non-weakening proven:** detector matrix unchanged (`TP=11 FP=0 TN=7 FN=0`), a clean node carrying an id-shaped `hash` (`h-foo-bar-1`) still writes + recalls, every secret shape still flags.

> Iteration honesty: my first fix fed `JSON.stringify(WHOLE PAYLOAD)` to the detector and it manufactured `high-entropy-token` FPs from JSON punctuation+keys (5 clean matrix cases over-blocked). Caught it by re-running the hardened gate (6 FAILs), root-caused to the `hash` value, and corrected to scan discrete content VALUES with a structural-key denylist. The detector itself was never weakened; only the gate's input surface changed.

**Detector adversarial sweep (`test/qa-detector-probe.mjs`, 19 cases): 0 secret leaks.** Every realistic evasion the test matrix did NOT cover is CAUGHT: full multiline PEM with base64 body, AWS 40-char **secret**-access-key (not the AKIA id) in an assignment, connection-string embedding a password, `.env` blob with multiple `KEY=secret` lines, JSON value holding a token, private-key assignment, bearer with odd case+spacing, base64 secret wrapped across newlines, URL-encoded token in a query string. **FP-pressure stayed low** — all 8 ordinary-content cases NOT flagged: long base64 PNG/JPEG fixtures, git SHA + sha256 digest in prose, UUIDs in prose, a long English paragraph, a Markdown table, minified JS, a stack trace. The one non-clean note (`GHP_...` UPPERCASE prefix flagged `high-entropy-token`) is a **correct conservative catch** of a 40-char opaque mixed-class token, NOT a false positive worth carving out — carving it would weaken the detector; left as-is by design.

**Chunk boundary (`test/qa-chunk-and-scale.mjs`): full-in-one ALWAYS refused; split is a pinned honest limitation.** A full secret inside ONE chunk → REFUSED, never in any recall snippet; the clean sibling chunk → written (not over-blocked). A secret deliberately SPLIT across a paragraph boundary (each half < the credential's min length) is per-chunk clean and both halves write — BUT recall **NEVER reassembles** them into one contiguous usable token (`B_fullSecretInAnySnippet=false`). Pinned: the same boundary that hides the split from the detector also prevents exfiltration via recall. Recorded as an asserted contract in `p3p4-qa.gate.mts` (QA-3 PINNED), not papered over.

**Consolidate at scale: idempotent + bounded + durable.** 40 same-session nodes, 6 forced sweeps → **edge count STABLE 780 across all 6** (780 = complete same-session graph 40×39/2; deterministic, bounded, NOT a runaway N²-of-everything explosion), run counter monotonic 2→7, **durable across a fresh reopen**, best-effort preserved (poisoned `execIr` never throws out of `sync()`/`consolidate`). finalTotal 840 (40 chunks + 780 edges + meta).

Added: `src/pluresdb.ts` — `#gateScanText` + `RECALL_CONTENT_FIELDS`/`STRUCTURAL_NONCONTENT_KEYS` shared constants (also points `deriveSnippet` at the shared field list so gate+recall never drift). `test/p3p4-qa.gate.mts` (28 self-asserting checks, exits non-zero on any breach) + vitest mirror in `test/recall.gate.test.ts` + `package.json` `test`/`test:p3p4-qa`. Adversarial probes kept as tools: `qa-detector-probe.mjs`, `qa-gate-field-probe.mjs`, `qa-chunk-and-scale.mjs`. No `node_modules`/`.node`/`dist` committed. No stubs (C-NOSTUB-001). No existing assertion weakened. The TEST-ONLY seams stayed test-only — no QA change leaked into shipped block logic; the only shipped change is broadening the gate's scan surface (strictly more secrets caught, no FP increase).

**Verdict: NO secret evades the gate on my run (the one real evasion found was FIXED for real + proven non-weakening); FP rate stayed low (8/8 ordinary-content cases clean); consolidation idempotent+bounded+durable at 40-node scale; full suite (build + 4 gates + vitest 5 + tsc) green.** QA gate: **PASS**.

## verify result line

**VERIFY ✓ (2026-06-29)** — The FINAL gate for P3+P4 and the last stage of EPIC-MEMORY-SUPERIORITY. Channel-agnostic consumer-boundary proof (C-TEST-002): drove the SHIPPED `MemorySearchManager` (`buildMemoryCapability().runtime.getMemorySearchManager().manager` → `sync()`/`search()`), NO chat adapter, NO mock, NO private-state peeking for the safety assertions; every durability claim reproduced across a genuinely FRESH process (`test/p3p4-verify-child.mts` per-phase). `pnpm run build` exit 0; `pnpm test` (recall.gate + p3p4.gate + p3p4-hardened.gate + p3p4-qa.gate + **new p3p4-verify.driver**) → **ALL CHECKS PASSED, exit 0**; `vitest run` → **6 passed, exit 0** (VERIFY mirror is the 6th); `tsc --noEmit` exit 0.

**PROOF 1 — C-MEM-REDACT END-TO-END (the headline safety proof):** a realistic batch synced via `manager.sync({sessionFiles})` — a clean memory + credentials in DIFFERENT fields: `content` (GitHub `ghp_`), a SECONDARY field `value` (AWS `AKIA`, the QA-fixed bug), a multiline PEM in `note`, an AWS 40-char secret in `body`. Write-accounting (`store.store()`): each secret `{written:0, refused:1}` with correct kind (`github-token`/`aws-access-key-id`/`pem-private-key`/`credential-assignment`), `get()===null`; clean `{written:1, refused:0}`, `get()!==null`. In a **FRESH process**, `manager.search()` proved **every credential node NEVER recalled** (`idRecalled=false`) and the **raw secret NEVER in any snippet** (`secretInSnippet=false` per-probe AND `anySecretAnywhere=false` under a broad catch-all query), while the **clean memory IS recalled**. The secondary-field case is a first-class explicit assertion. The live store cannot be made to persist+leak a secret through the shipped write/recall path.

**PROOF 2 — CONSOLIDATION REAL VALUE:** 24 same-session memories seeded via `manager.sync({force})`; 6 forced `consolidate()` sweeps → **edges STABLE at 276 = complete same-session graph (24×23/2)** (idempotent, bounded, no runaway), run counter **monotonic 2→7**, every sweep `ran:true`. **Durable across a FRESH process:** reopen → run **8** = prior **7** + 1, edges still **276** (`runCounterAdvanced=true`, `edgesStable=true`). **Observable value:** associative recall = **23 graph neighbors** of a direct hit, **before AND after** the restart — the structure persisted, not just a counter.

**PROOF 3 — NO REGRESSION:** recall GATE A/B/C/D + p3p4 + hardened (CONFUSION MATRIX TP=11 FP=0 TN=7 FN=0, secretLeaks=0) + p3p4-qa + the new verify driver ALL PASS on my own run; vitest 6/6; tsc clean.

**PROOF 4 — DURABILITY + DETERMINISM:** secret-block re-asserted in the FRESH recall process (nodes absent + never recalled after restart); consolidation checkpoint re-asserted in the FRESH reopen process (counter advanced, edges stable). Deterministic — same inputs, same 276-edge complete graph + monotonic counter on every run.

**HONESTY — PULL/TICK, not push:** P3's "reactive" sweep is a PULL/TICK sweep invoked opportunistically from `sync()`, NOT event-driven reactivity. The `@plures/pluresdb-native@2.0.0-alpha.1` Node binding has NO push path — a write does not auto-run a procedure, `subscribe()` is an id-only stub, procedures aren't executable via the binding. No self-firing/event-driven behavior is claimed anywhere. No recall miss was manufactured and no block was fabricated; the absent push path is reported as absent.

Added (test-only, no shipped/source change): `test/p3p4-verify-child.mts` (per-phase worker), `test/p3p4-verify.driver.mts` (4-proof self-asserting orchestrator), 6th vitest mirror in `test/recall.gate.test.ts`, `package.json` `test`/`test:p3p4-verify`. No `node_modules`/`.node`/`dist` committed. No stubs (C-NOSTUB-001). No existing assertion weakened.

**Verdict: BOTH P3 (PULL/TICK consolidation — bounded, idempotent, durable across restart, observable associative recall) and P4 (.px-governed write redaction — every credential field incl. the secondary-field case refused + never recalled + never leaked in a snippet, clean memory recalled) deliver real consumer-boundary value, proven on my own invocation across fresh processes, full regression suite green. EPIC-MEMORY-SUPERIORITY loop closed.** VERIFY gate: **PASS**.
